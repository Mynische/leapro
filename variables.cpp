#include <iostream>

int main()
{
   /* int x, y = 4; //declaration
    x = 5; //assignment
    int sum = x + y;

    // int x = t; declaration and assignment in a single step.
    std::cout << x << '\n';
    std::cout << y << '\n';
    */


    /* integer (whole number)
    int age = 21;
    int year = 2024;
    int days = 7.8;

    std::cout << days << '\n';
    */

    /* double (numbers including decimal)
    double price = 12.59;
    double gpa = 2.5;
    double temperature = 25.1;

   std::cout << price << '\n';
    */

   
    /* single character
    char grade = 'A';
    char inital = 'BC'; // char stores only one character, only the last character gets stored.
    char currency = '$'; // char can store special characters.

    std::cout << inital << '\n';
    std::cout << currency << '\n';
   */

  /* boolean ()
    bool student = false;
    bool power = true;
    bool forsale = true;

  */

    /* string (objects that represents a sequence of text)

    */
   std::string name = "Grisha";
   std::string day = "Monday";
   std::string food = "Apple";
   std::string address = "123 Shit Hell.";
   
   std::cout << name << '\n';
   std::cout << "Hello " << name << '\n';
   
   return 0;
}